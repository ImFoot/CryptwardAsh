You are building Cryptward: The Ashvault from the uploaded consolidated handoff. First read MASTER_SPECIFICATION.md. Treat it as the primary product and architecture authority. Then read config/game-config.json, config/generator-config.json, schemas/, levels/level-01-ashvault.json, templates/ashvault-room-catalog.json, and asset_manifest.json. JSON values win for exact data.

Use Phaser 3, TypeScript, and Vite. Build the milestones in order. Milestone A is the complete authored Ashvault vertical slice and must be playable using one thumb at 390 × 844 before any procedural floor is implemented. Architect a shared DungeonDefinition from the beginning. In Milestone B, normalize the authored Tiled map through that interface without changing gameplay. Only then implement the seeded room-template generator, validation, fallback seeds, Expedition unlock, and seed replay described in the specification.

Do not create separate combat systems for authored and generated maps. Do not substitute a random dungeon for the authored tutorial or fixed boss arena. Do not invent external services, logins, ads, purchases, multiplayer, or copied Gauntlet creative expression. Keep data local.

At the end of each milestone: run tests, launch the app, verify the mobile viewport, report changed files and known issues, and stop for approval if the workflow supports milestone approval. Required automated coverage includes gestures, authored map normalization, gates and shards, checkpoint restoration, deterministic seed output, 10,000-seed generation, inventory-state solvability, collision/object safety, fallback behavior, and generator timing.